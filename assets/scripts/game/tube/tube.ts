import { _decorator, Camera, Component, EventTouch, geometry, input, Input, Node, PhysicsSystem, SystemEvent } from 'cc';
const { ccclass, property } = _decorator;

@ccclass('tube')
export class tube extends Component {

    start () {
        
    }

    update(deltaTime: number) {
        
    }
}

